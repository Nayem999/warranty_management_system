name: 🚀 Deploy website on push

on:
  push:
    branches:
      - master

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2.3.2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: "goldeninfotech.net"
        username: "nayem"
        password: "Dfr#63gnym"
        port: "21"
        server-dir: "/Websites/snpapi.goldeninfotech.net/public_html/"

    # - name: 🔄 Run storage:link
    #   run: php artisan storage:link
